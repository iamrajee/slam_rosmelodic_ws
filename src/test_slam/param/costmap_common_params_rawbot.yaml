obstacle_range: 3.0 #1.0 # 2.0
raytrace_range: 3.5 #1.5 # 3.0

# robot_radius: 0.205 #0.3 # 0.0
footprint: [[-0.2, -0.15], [-0.2, 0.15], [0.2, 0.15], [0.2, -0.15]]
footprint_padding: 0.005

inflation_radius: 0.5 #1.0 #0.5 # 0.0
cost_scaling_factor: 3.0 #5.0

map_type: costmap
observation_sources: laser_scan_sensor
laser_scan_sensor: {sensor_frame: base_scan, data_type: LaserScan, topic: scan, marking: true, clearing: true}
# transform_tolerance: 0.5 # 0.0

# ===================== NEW ================== #
infation_layer:
  inflation_radius: 0.5
  
obstacle_layer:
  observation_sources: laser_scan_sensor
  laser_scan_sensor:
    sensor_frame: base_scan 
    data_type: LaserScan
    topic: scan
    marking: true
    clearing: true
  
range_sensor_layer:
  clear_threshold: 0.2
  mark_threshold:  0.80
  clear_on_max_reading: true
  topics: ["/fl_sonar", "/fr_sonar", "/bl_sonar", "/br_sonar"]
  # ===================== NEW ================== #